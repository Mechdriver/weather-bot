class ParserService:
    def __init__(self):
        self.weather_patterns = ["what's the weather in (.*)",
                                 "weather in (.*)",
                                 "(.*) weather"]

    def weather_find_location(message):
        pass
